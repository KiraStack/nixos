#!/bin/sh
# Hyprland screenshot script

# # Capture selected area (interactively)
# DIR="$HOME/Pictures/Screenshots"
# mkdir -p "$DIR"
# FILE="$DIR/screenshot_$(date +%Y%m%d_%H%M%S).png"
# grim -g "$(slurp)" "$FILE"
# wl-copy < "$FILE"

# Debug message
echo "Screenshot triggered at $(date +%H:%M:%S)"
